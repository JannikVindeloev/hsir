% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_hsi.R
"NAME"
subset_hsi

"TITLE"
Subset an hsi array while preserving attributes

"DESCRIPTION"
Efficiently subsets an hsi array while preserving all hsi-specific attributes 
(header, thumbnail, thumbnail_wavelength). This function is optimized for 
memory efficiency and speed by using direct array indexing without unnecessary copying.

"USAGE"
subset_hsi(x, x_range = NULL, y_range = NULL, wl_range = NULL, drop = FALSE)

"ARGUMENTS"
\item{x}{an hsi array object}
\item{x_range}{a vector of x indices or range (e.g., c(1, 10) or 1:10)}
\item{y_range}{a vector of y indices or range (e.g., c(1, 10) or 1:10)}
\item{wl_range}{a vector of wavelength indices or range (e.g., c(1, 10) or 1:10)}
\item{drop}{logical indicating whether to drop dimensions of length 1 (default: FALSE)}

"VALUE"
a subsetted hsi array with preserved attributes

"EXAMPLES"
\dontrun{
# Assuming sample_cube is an hsi object
subset_cube <- subset_hsi(sample_cube, x_range = 1:50, y_range = 1:50)

# Subset by wavelength range
subset_cube <- subset_hsi(sample_cube, wl_range = 1:10)

# Check that attributes are preserved
print(subset_cube)
}

"SEE ALSO"
\link{read_pam}
